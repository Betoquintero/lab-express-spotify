<div>
    {{#each items}}
    <h2> name: {{ name }}</h2>    
    {{#each images}}
    <p>image: <img src = "{{ url }}"  alt="{{ name }}"/></p>
    {{/each}} 
    <a href="/tracks/{{id}}">View Tracks</a>
    {{/each}}   
</div>